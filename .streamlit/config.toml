# GUG CRM — Streamlit app config (safe to commit, no secrets here).
#
# primaryColor matches the app's own brand blue (app.py's COLOR_BLUE, light
# mode). Base/background intentionally left unset so light/dark keeps
# following the visitor's system preference, same as the rest of the app's
# theme-adaptive design (app._theme_type()).
[theme]
primaryColor = "#2a78d6"
