# GUG CRM — secrets template. Copy this to `secrets.toml` for local runs
# (gitignored, never committed). For Streamlit Community Cloud, paste this
# same content — with your real password — into the app's own
# Settings → Secrets box instead; you do not upload this file there.
admin_password = "choose-a-real-password-here"
